var katzDeli = [];
var newCust = 'Ada'

function takeANumber(katzDeli, newCust) {
  katzDeli = katzDeli.push('newCust')
  var pos = katzDeli.indexOf('newCust') +1 ;
  return "Welcome, $newCust. You are parseInt(pos) in line."
  
}

function currentLine(katzDeli) {
  if (katzDeli.length === 0) {
    return "The line is currently empty."
  } else {
    
  }
}